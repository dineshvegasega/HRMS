<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!--  Background color -->
    <corners  android:bottomLeftRadius="@dimen/_20sdp"
        android:bottomRightRadius="@dimen/_20sdp"/>
    <padding
        android:bottom="-10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"/>
    <item>
        <shape>
            <solid android:color="#80000000"/> <!-- Shadow color (black with transparency) -->
            <corners android:radius="8dp"/>
            <padding
                android:bottom="-10dp"
                android:left="10dp"
                android:right="10dp"
                android:top="10dp"/>
            <size
                android:width="100dp"
                android:height="100dp"/> <!-- Adjust size to your needs -->
        </shape>
    </item>
</shape>